export interface IProduct{

    id : number,
    name ? : string,
    category? : string,
    price ?: number,
    description ?: string,
    image ?:string,
    rating ?:number,
    discount ?:number,
    reviewCount?:number 

}